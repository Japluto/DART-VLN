那就先跑这 6 组，最省算力，也最容易看出趋势。

先进入目录：

```bash
cd /home/japluto/VLN/GridMM_ff/map_nav_src
```

**1. 基线**
```bash
OUTDIR=/home/japluto/VLN/GridMM_ff/default/r2r_tune_off \
DYNAMIC_MEMORY_MODE=off \
bash scripts/run_r2r.sh test
```

**2. 只测弱遗忘**
```bash
OUTDIR=/home/japluto/VLN/GridMM_ff/default/r2r_tune_decay_005 \
DYNAMIC_MEMORY_MODE=decay_only \
DYNAMIC_MEMORY_EXTRA_ARGS="--dynamic_memory_decay_lambda 0.05 --dynamic_memory_min_mem_weight 0.35" \
bash scripts/run_r2r.sh test
```

**3. 只测中等遗忘**
```bash
OUTDIR=/home/japluto/VLN/GridMM_ff/default/r2r_tune_decay_012 \
DYNAMIC_MEMORY_MODE=decay_only \
DYNAMIC_MEMORY_EXTRA_ARGS="--dynamic_memory_decay_lambda 0.12 --dynamic_memory_min_mem_weight 0.35" \
bash scripts/run_r2r.sh test
```

**4. 只测保守更新**
```bash
OUTDIR=/home/japluto/VLN/GridMM_ff/default/r2r_tune_update_gate_008 \
DYNAMIC_MEMORY_MODE=update_only \
DYNAMIC_MEMORY_EXTRA_ARGS="--dynamic_memory_base_gate 0.08 --dynamic_memory_match_radius 0.75 --dynamic_memory_novelty_weight 0.35 --dynamic_memory_age_weight 0.20 --dynamic_memory_repeat_weight 0.15" \
bash scripts/run_r2r.sh test
```

**5. 只测默认更新**
```bash
OUTDIR=/home/japluto/VLN/GridMM_ff/default/r2r_tune_update_gate_015 \
DYNAMIC_MEMORY_MODE=update_only \
DYNAMIC_MEMORY_EXTRA_ARGS="--dynamic_memory_base_gate 0.15 --dynamic_memory_match_radius 0.75 --dynamic_memory_novelty_weight 0.35 --dynamic_memory_age_weight 0.20 --dynamic_memory_repeat_weight 0.15" \
bash scripts/run_r2r.sh test
```

**6. 只测更宽合并半径**
```bash
OUTDIR=/home/japluto/VLN/GridMM_ff/default/r2r_tune_update_radius_100 \
DYNAMIC_MEMORY_MODE=update_only \
DYNAMIC_MEMORY_EXTRA_ARGS="--dynamic_memory_base_gate 0.15 --dynamic_memory_match_radius 1.0 --dynamic_memory_novelty_weight 0.35 --dynamic_memory_age_weight 0.20 --dynamic_memory_repeat_weight 0.15" \
bash scripts/run_r2r.sh test
```

**怎么解读这 6 组**
- `2 vs 3 vs 1`
  看 `decay_lambda` 有没有正收益
- `4 vs 5`
  看 `base_gate` 偏小还是默认更好
- `6 vs 5`
  看更大 `match_radius` 是帮助去冗余，还是造成误合并

**我最关注的指标**
- `sr`
- `spl`
- `nav_error`
- `nDTW`

你跑完把结果贴给我，我可以直接帮你判断下一轮该往哪个方向继续缩小范围。